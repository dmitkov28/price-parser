package templates

templ PriceTag(price string) {
    <span class="inline-flex items-center px-3 py-1 rounded-full bg-blue-500 text-white text-sm font-medium border border-blue-700 shadow-md">
        { price } лв.
    </span>
}
